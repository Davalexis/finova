import 'package:flutter/widgets.dart';

class ErrorScreen extends StatelessWidget {
  const ErrorScreen(String string, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}