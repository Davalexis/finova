import 'package:flutter/material.dart';

class DataPlanScreen extends StatelessWidget {
  const DataPlanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
